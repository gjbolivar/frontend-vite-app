import React from 'react';

const InventorySidebar = () => {
  // Este componente ya no es necesario como sidebar,
  // su contenido se ha movido a InventoryNavBar.
  // Se mantiene para evitar errores de importación, pero no renderiza nada.
  return (
    <></>
  );
};

export default InventorySidebar;


// DONE